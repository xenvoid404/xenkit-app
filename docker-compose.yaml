services:
    xenkit-app:
        container_name: xenkit-app
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        networks:
            - app-network

networks:
    app-network:
        driver: bridge
